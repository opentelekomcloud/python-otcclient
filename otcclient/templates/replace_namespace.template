{ 
    "apiVersion": "v1", 
    "kind": "Namespace", 
    "metadata": { 
        "name": "{{NAMESPACE}}",
        "labels": {
            "name": "development"
        }
    },
    "spec": { 
        "finalizers": [ 
            "kubernetes"
        ] 
    }
}